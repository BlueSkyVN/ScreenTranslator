<Window x:Class="ScreenTranslator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Screen Translator Pro" Height="700" Width="480"
        WindowStartupLocation="CenterScreen" Background="#0F0F11" Foreground="#F4F4F5">
    <Window.Resources>
        <!-- Modern Card Style -->
        <Style x:Key="CardBorder" TargetType="Border">
            <Setter Property="Background" Value="#18181B"/>
            <Setter Property="BorderBrush" Value="#27272A"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Margin" Value="0,0,0,16"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Black" BlurRadius="8" ShadowDepth="2" Opacity="0.3"/>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Modern Button Style -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#4F46E5"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Height" Value="34"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#6366F1"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#4338CA"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#27272A"/>
            <Setter Property="BorderBrush" Value="#3F3F46"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="#E4E4E7"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#3F3F46"/>
                    <Setter Property="BorderBrush" Value="#52525B"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Form controls styling -->
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#09090B"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#27272A"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="CaretBrush" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="true">
                                <Setter TargetName="border" Property="BorderBrush" Value="#6366F1"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ToggleButton Style for ComboBox -->
        <Style x:Key="ComboBoxToggleButton" TargetType="ToggleButton">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="Border" Background="#09090B" BorderBrush="#27272A" BorderThickness="1" CornerRadius="4">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition />
                                    <ColumnDefinition Width="30" />
                                </Grid.ColumnDefinitions>
                                <Path x:Name="Arrow" Grid.Column="1" Fill="#A1A1AA" HorizontalAlignment="Center" VerticalAlignment="Center" Data="M 0 0 L 4 4 L 8 0 Z"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#18181B"/>
                                <Setter TargetName="Border" Property="BorderBrush" Value="#3F3F46"/>
                                <Setter TargetName="Arrow" Property="Fill" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="#6366F1"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ComboBox Style -->
        <Style TargetType="ComboBox">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <!-- ToggleButton acts as the background and dropdown arrow -->
                            <ToggleButton Name="ToggleButton" 
                                          Style="{StaticResource ComboBoxToggleButton}" 
                                          Focusable="false"
                                          IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"/>
                            
                            <!-- ContentPresenter shows the selected item -->
                            <ContentPresenter Name="ContentSite" 
                                              IsHitTestVisible="False" 
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                              Margin="{TemplateBinding Padding}"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Left"/>
                            
                            <!-- Popup containing the ScrollViewer and ItemsPresenter -->
                            <Popup Name="Popup" 
                                   Placement="Bottom" 
                                   IsOpen="{TemplateBinding IsDropDownOpen}" 
                                   AllowsTransparency="True" 
                                   Focusable="False" 
                                   PopupAnimation="Slide">
                                <Grid Name="DropDown" 
                                      SnapsToDevicePixels="True" 
                                      MinWidth="{TemplateBinding ActualWidth}" 
                                      MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border x:Name="DropDownBorder" 
                                            Background="#18181B" 
                                            BorderBrush="#27272A" 
                                            BorderThickness="1" 
                                            CornerRadius="6"
                                            Margin="0,2,0,4">
                                        <Border.Effect>
                                            <DropShadowEffect Color="Black" BlurRadius="6" ShadowDepth="2" Opacity="0.4" />
                                        </Border.Effect>
                                        <ScrollViewer Margin="2" SnapsToDevicePixels="True" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" CanContentScroll="True">
                                            <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained" />
                                        </ScrollViewer>
                                    </Border>
                                </Grid>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ComboBoxItem Style -->
        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#E4E4E7"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Border" 
                                Background="{TemplateBinding Background}" 
                                Padding="{TemplateBinding Padding}" 
                                CornerRadius="4" 
                                Margin="2,1"
                                SnapsToDevicePixels="true">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#27272A"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#4F46E5"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="#E4E4E7"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="SCREEN TRANSLATOR PRO" FontSize="20" FontWeight="Black">
                <TextBlock.Foreground>
                    <LinearGradientBrush EndPoint="1,0" StartPoint="0,0">
                        <GradientStop Color="#818CF8" Offset="0"/>
                        <GradientStop Color="#34D399" Offset="1"/>
                    </LinearGradientBrush>
                </TextBlock.Foreground>
            </TextBlock>
            <TextBlock Text="Hệ thống dịch màn hình thông minh thời gian thực (MVVM)" FontSize="11" Foreground="#71717A" Margin="0,2,0,0"/>
        </StackPanel>

        <!-- Main Body (Scrollable) -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="0,0,8,0">
            <StackPanel>
                <!-- Card 1: Connection & API settings -->
                <Border Style="{StaticResource CardBorder}">
                    <StackPanel>
                        <TextBlock Text="CẤU HÌNH KẾT NỐI AI" FontSize="13" FontWeight="Bold" Foreground="#818CF8" Margin="0,0,0,12"/>

                        <StackPanel Margin="0,0,0,10">
                            <TextBlock Text="API Key:" FontSize="11" Foreground="#A1A1AA" Margin="0,0,0,4"/>
                            <TextBox Text="{Binding ApiKey, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" TextWrapping="NoWrap"/>
                        </StackPanel>

                        <StackPanel Margin="0,0,0,12">
                            <TextBlock Text="Bộ máy Dịch thuật (AI Model):" FontSize="11" Foreground="#A1A1AA" Margin="0,0,0,4"/>
                            <ComboBox SelectedValuePath="Tag" SelectedValue="{Binding ApiModel, Mode=TwoWay}">
                                <ComboBoxItem Content="Google Dịch Truyền thống (Miễn phí &amp; Không giới hạn)" Tag="free_google|none"/>
                                <ComboBoxItem Content="Dịch thuật Ngoại tuyến (Local Offline - Cần mô hình)" Tag="local_offline|none"/>
                                <ComboBoxItem Content="Google Gemini 1.5 Flash (Ổn định nhất - Khuyên dùng)" Tag="gemini|gemini-1.5-flash"/>
                                <ComboBoxItem Content="Google Gemini 2.5 Flash (Thử nghiệm - Tốc độ cao)" Tag="gemini|gemini-2.5-flash"/>
                                <ComboBoxItem Content="Google Gemini 1.5 Pro (Cao cấp - Dịch mượt)" Tag="gemini|gemini-1.5-pro"/>
                                <ComboBoxItem Content="Groq Llama 3.1 8B (Tối giản - Rất nhanh)" Tag="groq|llama-3.1-8b-instant"/>
                                <ComboBoxItem Content="Groq Llama 3.3 70B (Trung bình - Dịch chuẩn)" Tag="groq|llama-3.3-70b-versatile"/>
                                <ComboBoxItem Content="OpenAI GPT-4o-mini (Cực tốt - Trễ cao)" Tag="openai|gpt-4o-mini"/>
                            </ComboBox>
                        </StackPanel>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Button Grid.Column="0" Content="Thử kết nối (Test)" Style="{StaticResource SecondaryButton}" Command="{Binding TestConnectionCommand}"/>
                            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="12,0,0,0">
                                <TextBlock Text="Trạng thái: " Foreground="#71717A" FontSize="11"/>
                                <TextBlock Text="{Binding ConnectionStatusText}" Foreground="{Binding ConnectionStatusColor}" FontWeight="Bold" FontSize="11"/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Card 2: Controller -->
                <Border Style="{StaticResource CardBorder}">
                    <StackPanel>
                        <TextBlock Text="ĐIỀU KHIỂN CHỤP VÀ DỊCH" FontSize="13" FontWeight="Bold" Foreground="#34D399" Margin="0,0,0,12"/>

                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Button Grid.Column="0" Content="Chọn vùng quét" Style="{StaticResource ModernButton}" Command="{Binding SelectRegionCommand}" Margin="0,0,6,0"/>
                            <CheckBox Grid.Column="1" Content="Kích hoạt Dịch Real-time" IsChecked="{Binding IsRunning, Mode=TwoWay}" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="12" FontWeight="Bold"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Card 3: Overlay Appearance Customizer -->
                <Border Style="{StaticResource CardBorder}">
                    <StackPanel>
                        <TextBlock Text="TÙY CHỈNH PHỤ ĐỀ (OVERLAY)" FontSize="13" FontWeight="Bold" Foreground="#F59E0B" Margin="0,0,0,12"/>

                        <!-- Opacity Slider -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="50"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Độ mờ nền phụ đề (Opacity):" FontSize="11" Foreground="#A1A1AA" Margin="0,0,0,4"/>
                                <Slider Minimum="0.0" Maximum="1.0" Value="{Binding OverlayOpacity, Mode=TwoWay}" TickFrequency="0.1" IsSnapToTickEnabled="True"/>
                            </StackPanel>
                            <TextBlock Grid.Column="1" Text="{Binding OverlayOpacity, StringFormat={}{0:P0}}" VerticalAlignment="Bottom" HorizontalAlignment="Right" FontSize="12" Foreground="White" FontWeight="Bold" Margin="0,0,0,2"/>
                        </Grid>

                        <!-- Font Size Slider -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="50"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Cỡ chữ phụ đề (Font Size):" FontSize="11" Foreground="#A1A1AA" Margin="0,0,0,4"/>
                                <Slider Minimum="16" Maximum="48" Value="{Binding OverlayFontSize, Mode=TwoWay}" TickFrequency="2" IsSnapToTickEnabled="True"/>
                            </StackPanel>
                            <TextBlock Grid.Column="1" Text="{Binding OverlayFontSize}" VerticalAlignment="Bottom" HorizontalAlignment="Right" FontSize="12" Foreground="White" FontWeight="Bold" Margin="0,0,0,2"/>
                        </Grid>

                        <!-- Color & Target Language -->
                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" Margin="0,0,6,0">
                                <TextBlock Text="Màu chữ:" FontSize="11" Foreground="#A1A1AA" Margin="0,0,0,4"/>
                                <ComboBox SelectedValuePath="Tag" SelectedValue="{Binding OverlayTextColor, Mode=TwoWay}">
                                    <ComboBoxItem Content="Neon Green" Tag="#00FF00"/>
                                    <ComboBoxItem Content="Emerald Green" Tag="#10B981"/>
                                    <ComboBoxItem Content="Golden Yellow" Tag="#F59E0B"/>
                                    <ComboBoxItem Content="Cyan Sky" Tag="#06B6D4"/>
                                    <ComboBoxItem Content="Ivory White" Tag="#F4F4F5"/>
                                    <ComboBoxItem Content="Soft Rose" Tag="#FB7185"/>
                                </ComboBox>
                            </StackPanel>
                            <StackPanel Grid.Column="1" Margin="6,0,0,0">
                                <TextBlock Text="Dịch sang ngôn ngữ:" FontSize="11" Foreground="#A1A1AA" Margin="0,0,0,4"/>
                                <ComboBox SelectedValuePath="Tag" SelectedValue="{Binding TargetLang, Mode=TwoWay}">
                                    <ComboBoxItem Content="Tiếng Việt (vi)" Tag="vi"/>
                                    <ComboBoxItem Content="Tiếng Anh (en)" Tag="en"/>
                                    <ComboBoxItem Content="Tiếng Nhật (ja)" Tag="ja"/>
                                    <ComboBoxItem Content="Tiếng Hàn (ko)" Tag="ko"/>
                                    <ComboBoxItem Content="Tiếng Trung (zh-CN)" Tag="zh-CN"/>
                                </ComboBox>
                            </StackPanel>
                        </Grid>

                        <!-- Click-through lock -->
                        <CheckBox Content="Khóa tương tác phụ đề (Click-Through Lock)" IsChecked="{Binding LockOverlayClickThrough, Mode=TwoWay}" Margin="0,4,0,0" ToolTip="Khi khóa, phụ đề sẽ bỏ qua mọi tương tác chuột (xuyên chuột), cho phép click trực tiếp vào game/video bên dưới!"/>
                    </StackPanel>
                </Border>

                <!-- Card 4: Diagnostics -->
                <Border Style="{StaticResource CardBorder}">
                    <StackPanel>
                        <TextBlock Text="TRẠNG THÁI HỆ THỐNG" FontSize="13" FontWeight="Bold" Foreground="#EC4899" Margin="0,0,0,12"/>

                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Trạng thái dịch:" Foreground="#A1A1AA" FontSize="11"/>
                            <TextBlock Grid.Column="1" Text="{Binding StatusText}" Foreground="{Binding StatusColor}" FontWeight="Bold" FontSize="11" Margin="6,0,0,0"/>
                        </Grid>

                        <TextBlock Text="Văn bản nhận dạng (OCR):" FontSize="11" Foreground="#A1A1AA" Margin="0,0,0,4"/>
                        <Border Background="#09090B" BorderBrush="#27272A" BorderThickness="1" CornerRadius="4" Padding="10" MinHeight="50">
                            <TextBlock Text="{Binding LastOcrText}" TextWrapping="Wrap" Foreground="#D4D4D8" FontSize="11"/>
                        </Border>
                    </StackPanel>
                </Border>

                <!-- Footer -->
                <TextBlock Text="Screen Translator Pro • Phiên bản 2.0" HorizontalAlignment="Center" FontSize="10" Foreground="#3F3F46" Margin="0,8,0,16"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>
