<Window x:Class="ScreenTranslator.UI.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Overlay UI" SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False"
        MouseDown="Window_MouseDown" Cursor="Hand">
    <Grid>
        <Border x:Name="OverlayBorder" Background="#CC000000" CornerRadius="10" Padding="15" Margin="10">
            <Border.Effect>
                <DropShadowEffect Color="Black" BlurRadius="10" ShadowDepth="3" Opacity="0.5" />
            </Border.Effect>
            <TextBlock x:Name="TranslatedText" 
                       Text="[Bản dịch sẽ hiển thị ở đây. Kéo thả để di chuyển]" 
                       Foreground="#00FF00" 
                       FontSize="26" 
                       FontWeight="Bold"
                       MaxWidth="800"
                       TextWrapping="Wrap"
                       TextAlignment="Center"
                       HorizontalAlignment="Center" 
                       VerticalAlignment="Center">
            </TextBlock>
        </Border>
    </Grid>
</Window>
